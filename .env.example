# Copie para .env na raiz do projeto e ajuste em produção.

# API local (MD04) — preferir também apps/server/.env ao rodar só o Nest
PORT=13050
DATABASE_URL=file:./apps/server/prisma/dev.db
SECRET_KEY=change-me-in-production
